# 🎛️ Адмін Панель - Інструкція

Адмін панель дозволяє легко додавати нові 360° фото навіть людям без технічних навичок.

## 🌐 Доступ

**URL:** https://vr-photo.pages.dev/admin/

**Пароль (за замовчуванням):** `war-in-vr-2024`

⚠️ **ВАЖЛИВО:** Змініть пароль в `admin/app.js` (рядок 3)

---

## 📸 Як додати нову 360° панораму

### Крок 1: Обробка зображення

1. Відкрийте https://vr-photo.pages.dev/admin/
2. Введіть пароль
3. Оберіть номер нової сцени (наприклад, 13)
4. Перетягніть 360° фото (або натисніть "Обрати файл")
5. Почекайте 30-60 секунд - панель автоматично:
   - ✅ Створить мобільну версію (2048×1024 WebP)
   - ✅ Створить десктоп версію (4096×2048 WebP)
   - ✅ Створить VR версію (8192×4096 JPEG)
   - ✅ Завантажить всі 3 файли на ваш комп'ютер

### Крок 2: Завантаження в R2

**Варіант А: Через термінал (рекомендовано)**

```bash
cd /Users/alex/Documents/GitHub/war-in-vr
./admin/upload-to-r2.sh 13 scene-13-mobile.webp scene-13-desktop.webp scene-13-vr.jpg
```

**Варіант Б: Через Cloudflare Dashboard**

1. Відкрийте https://dash.cloudflare.com → R2 → warinvr-panoramas
2. Створіть папку `13/picture/`
3. Завантажте файли:
   - `mobile.webp` → `13/picture/mobile.webp`
   - `desktop.webp` → `13/picture/desktop.webp`
   - `vr.jpg` → `13/picture/1.jpg` ⚠️ (важливо: назва `1.jpg`)

### Крок 3: Створити HTML сторінку

**Якщо додаєте нову сцену (не оновлюєте існуючу):**

1. Скопіюйте папку `1/` → `13/`
2. Відредагуйте `13/index.html`:
   - Рядок 39: змініть `src="...5/picture/1.jpg"` на `src="...13/picture/1.jpg"`
   - Рядок 47: додайте `active` класс до кнопки `<a href="../13/" class="nav-number active">13</a>`
3. Оновіть головну сторінку `index.html` - додайте карточку для сцени 13

### Крок 4: Deploy

```bash
git add -A
git commit -m "Add scene 13"
git push
```

Cloudflare Pages автоматично задеплоїть через 1-2 хвилини! ✅

---

## 🔒 Безпека

**НІКОЛИ не зберігайте R2 credentials в `admin/app.js`!**

Зараз адмін панель завантажує файли локально (на ваш комп'ютер), а ви завантажуєте їх в R2 через:
- ✅ AWS CLI (через термінал) - безпечно
- ✅ Cloudflare Dashboard - безпечно
- ❌ НЕ через браузер з credentials в коді - небезпечно!

---

## 🔧 Альтернатива: Cloudflare Worker (для production)

Якщо хочете повністю автоматичний upload через веб-інтерфейс, потрібно створити Cloudflare Worker:

```javascript
// worker.js (приклад)
export default {
  async fetch(request, env) {
    // Автентифікація
    // Приймає файли з адмін панелі
    // Завантажує в R2 через env.R2_BUCKET
    // Повертає результат
  }
}
```

Докладніше: https://developers.cloudflare.com/r2/examples/demo-worker/

---

## 📞 Підтримка

Якщо виникли проблеми:
1. Перевірте що AWS CLI налаштований (`~/.aws/credentials` містить `[r2]` профіль)
2. Перевірте CORS в R2 bucket (Settings → CORS Policy)
3. Перевірте що Public Development URL увімкнений

---

**Успіхів! 🚀**
