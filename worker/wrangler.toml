name = "war-in-vr-upload"
main = "worker.js"
compatibility_date = "2024-01-01"

# R2 bucket binding
[[r2_buckets]]
binding = "R2_BUCKET"
bucket_name = "warinvr-panoramas"

# Environment variables (встановіть через Dashboard або wrangler secret)
[vars]
PUBLIC_URL = "https://pub-21040fd818d4437484f8a3c1ca05743a.r2.dev"

# Secret змінні (встановіть через: wrangler secret put ADMIN_PASSWORD)
# ADMIN_PASSWORD = "your-password-here" (не зберігайте тут!)
