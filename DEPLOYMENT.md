# 🚀 Інструкція з деплою War in VR

## 📋 Передумови

1. ✅ Акаунт на Cloudflare (безкоштовний)
2. ✅ Git встановлений (опціонально)
3. ✅ Node.js встановлений (опціонально, для Wrangler CLI)

---

## 🎯 ШВИДКИЙ СТАРТ (5 хвилин)

### Крок 1: Створити акаунт Cloudflare

1. Зайдіть на https://dash.cloudflare.com/sign-up
2. Зареєструйтесь (email + пароль)
3. Підтвердіть email

### Крок 2: Деплой на Cloudflare Pages

**Варіант А: Через веб-інтерфейс (найпростіше)**

1. Зайдіть на https://pages.cloudflare.com
2. Натисніть "Create a project"
3. Виберіть "Upload assets"
4. Перетягніть папку `war-in-vr` (або виберіть файли)
5. Project name: `war-in-vr` (або інша назва)
6. Натисніть "Deploy"
7. ✅ Готово! Отримаєте URL: `https://war-in-vr.pages.dev`

**Варіант Б: Через Git (рекомендовано)**

1. Створіть GitHub репозиторій
2. Завантажте код:
   ```bash
   git init
   git add .
   git commit -m "Initial commit"
   git remote add origin YOUR_GITHUB_URL
   git push -u origin main
   ```
3. На Cloudflare Pages: "Connect to Git"
4. Виберіть репозиторій
5. Build settings:
   - Build command: (leave empty)
   - Build output directory: `/`
6. Deploy!

---

## 🗄️ НАЛАШТУВАННЯ R2 STORAGE

### Крок 1: Створити R2 Bucket

1. В Cloudflare Dashboard → R2
2. Натисніть "Create bucket"
3. Назва: `warinvr-panoramas` (або інша)
4. Location: Automatic (рекомендовано)
5. Create bucket

### Крок 2: Отримати API credentials

1. R2 → Manage R2 API Tokens
2. Create API Token
3. Налаштування:
   - Token name: `war-in-vr-admin`
   - Permissions: Object Read & Write
   - TTL: Forever (або обрати термін)
4. Create API Token
5. ⚠️ **ВАЖЛИВО:** Збережіть:
   - Access Key ID
   - Secret Access Key
   - (Більше не зможете побачити Secret Key!)

### Крок 3: Налаштувати публічний доступ

1. Відкрийте bucket `warinvr-panoramas`
2. Settings → Public access
3. Опції:
   - **Варіант А:** Allow Access (через r2.dev)
     - Отримаєте URL: `https://pub-xxxxx.r2.dev`
     - Безкоштовно, працює одразу
   
   - **Варіант Б:** Custom domain
     - Додати свій піддомен (cdn.warinvr.com)
     - Налаштувати DNS
     - Безкоштовно, більш професійно

### Крок 4: Оновити конфігурацію

Відредагуйте файл `admin/app.js`:

```javascript
const CONFIG = {
    ADMIN_PASSWORD: 'your-secure-password-here',
    
    R2_ENDPOINT: 'https://YOUR-ACCOUNT-ID.r2.cloudflarestorage.com',
    R2_BUCKET: 'warinvr-panoramas',
    R2_ACCESS_KEY_ID: 'YOUR_ACCESS_KEY_ID_HERE',
    R2_SECRET_ACCESS_KEY: 'YOUR_SECRET_ACCESS_KEY_HERE',
    
    PUBLIC_URL: 'https://pub-xxxxx.r2.dev' // або custom domain
};
```

⚠️ **БЕЗПЕКА:** Не публікуйте ці credentials на GitHub!

---

## 🔐 БЕЗПЕЧНЕ ЗБЕРІГАННЯ CREDENTIALS

### Варіант 1: Environment Variables (рекомендовано)

1. Cloudflare Pages → Settings → Environment variables
2. Додайте:
   - `R2_ACCESS_KEY_ID`
   - `R2_SECRET_ACCESS_KEY`
   - `ADMIN_PASSWORD`
3. Оновіть `admin/app.js` для читання з `window.ENV`

### Варіант 2: Cloudflare Workers

Створіть Worker для проксі-upload (більш безпечно):

```javascript
// worker.js
export default {
  async fetch(request, env) {
    // Автентифікація
    // Проксі до R2
    // Повернути результат
  }
}
```

---

## 🌐 НАЛАШТУВАННЯ ДОМЕНУ

### Безкоштовний піддомен (автоматично)

✅ Вже є: `war-in-vr.pages.dev`

### Власний домен

1. Cloudflare Pages → Custom domains
2. Add domain → введіть `warinvr.com`
3. Cloudflare автоматично налаштує DNS
4. Чекайте 5-10 хвилин на активацію SSL
5. ✅ Готово!

---

## 📱 QR-КОДИ

### Генерація QR-кодів для всіх сцен:

```bash
# Online генератори:
https://qr-code-generator.com
https://www.qrcode-monkey.com

# URL для QR-кодів:
https://war-in-vr.pages.dev/1/
https://war-in-vr.pages.dev/2/
...
https://war-in-vr.pages.dev/20/
```

### Друк QR-кодів:

1. Згенеруйте всі коди
2. Додайте підпис "Сцена 1", "Сцена 2" тощо
3. Роздрукуйте на міцному папері
4. (Опціонально) Заламінуйте для довговічності

---

## 🧪 ТЕСТУВАННЯ

### Локальне тестування:

```bash
# Простий HTTP сервер:
python3 -m http.server 8000
# або
npx http-server -p 8000

# Відкрийте:
http://localhost:8000
```

### Тестування адмін панелі:

1. Відкрийте: `http://localhost:8000/admin/`
2. Введіть пароль
3. Оберіть тестове фото
4. Перевірте обробку
5. (Завантаження буде локальне, поки не налаштуєте R2)

---

## 🔧 TROUBLESHOOTING

### Проблема: "Access Denied" при upload

**Рішення:**
- Перевірте R2 API credentials
- Перевірте CORS налаштування в R2 bucket
- Перевірте permissions API token

### Проблема: Повільне завантаження панорам

**Рішення:**
- Перевірте розмір файлів (повинні бути < 5 MB для mobile)
- Увімкніть R2 public access для CDN
- Перевірте формат WebP (швидший за JPEG)

### Проблема: VR не працює на мобільному

**Рішення:**
- Перевірте HTTPS (обов'язково для VR)
- Перевірте версію A-Frame
- Перевірте permissions камери/гіроскопа

---

## 📊 МОНІТОРИНГ

### Cloudflare Analytics (безкоштовно):

1. Pages → Analytics
2. Дивіться:
   - Кількість візитів
   - Bandwidth використання
   - Популярні сцени
   - Географія користувачів

### R2 Usage:

1. R2 → Usage
2. Дивіться:
   - Storage (MB)
   - Requests count
   - Bandwidth

---

## 🚀 ОНОВЛЕННЯ САЙТУ

### Через Git:

```bash
# Зробіть зміни в коді
git add .
git commit -m "Update scene 12"
git push

# Cloudflare автоматично задеплоїть!
```

### Через веб-інтерфейс:

1. Pages → Deployments
2. "Upload new assets"
3. Перетягніть оновлені файли
4. Deploy

---

## 💰 ВАРТІСТЬ

```
Cloudflare Pages:  $0/міс
R2 Storage:        $0/міс (< 10 GB)
R2 Bandwidth:      $0/міс (необмежений)
Custom domain:     $9-12/рік (опціонально)

РАЗОМ: $0-1/міс
```

---

## 📞 ПІДТРИМКА

- Cloudflare Docs: https://developers.cloudflare.com/pages/
- R2 Docs: https://developers.cloudflare.com/r2/
- Community: https://community.cloudflare.com/

---

**Готово! Тепер у вас є повноцінний VR-сайт з адмін панеллю! 🎉**
